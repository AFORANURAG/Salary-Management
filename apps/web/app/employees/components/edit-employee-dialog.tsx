import type { Employee } from "@salary-mgmt/types";

// stub — implemented in feat/employees-fe-pr4-forms
export function EditEmployeeDialog(_props: {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  employee: Employee;
}) {
  return null;
}
